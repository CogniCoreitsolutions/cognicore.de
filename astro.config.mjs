// @ts-check
import { defineConfig } from 'astro/config';

// https://astro.build/config
export default defineConfig({
  // Staging-Domain (GitHub Pages). Zum Go-live auf https://cognicore.de umstellen.
  site: 'https://new.cognicore.de',
});
